export class Transaction {
    constructor(
        id: Number,
        amount: Number,
        date: String,
        description: String,
        into_account: Number,
        into_goal: Number,
        out_of_account: Number,
        transactionType: String
    ){}
}